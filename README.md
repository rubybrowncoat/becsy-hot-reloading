# Steps

pnpm:

- `pnpm install`
- `pnpm dev`
- navigate to http://localhost:8080
- uncomment line 128 in `src/lib/becsy.ts`

npm:

- `npm install`
- `npm run dev`
- navigate to http://localhost:8080
- uncomment line 128 in `src/lib/becsy.ts`
